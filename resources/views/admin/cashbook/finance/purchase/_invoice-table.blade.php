<section class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="border-b border-slate-200 p-4"><h2 class="font-black text-slate-950">{{ $title }}</h2></div><div class="overflow-x-auto"><table class="w-full text-left text-xs"><thead class="bg-slate-50 text-[10px] uppercase text-slate-500"><tr><th class="p-3">Date</th><th class="p-3">Invoice</th><th class="p-3">Vendor / Purchaser</th><th class="p-3">Method</th><th class="p-3 text-right">Purchase</th><th class="p-3 text-right">Outstanding</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($invoices as $invoice)<tr><td class="p-3 font-mono">{{ $invoice->business_date }}</td><td class="p-3 font-bold"><a class="text-emerald-700 hover:underline" href="{{ route('purchasing.invoices.show', $invoice->invoice_public_uuid) }}">{{ $invoice->invoice_number ?: 'Invoice' }}</a></td><td class="p-3">{{ $invoice->supplier_name }}<span class="block text-slate-400">{{ $invoice->purchaser_name }}</span></td><td class="p-3 uppercase">{{ $invoice->payment_class }}</td><td class="p-3 text-right font-mono">₹{{ number_format((float) $invoice->total_purchase, 2) }}</td><td class="p-3 text-right font-mono">₹{{ number_format((float) $invoice->outstanding, 2) }}</td></tr>@empty<tr><td colspan="6" class="p-5 text-center text-slate-400">No matching invoices.</td></tr>@endforelse</tbody></table></div>@if($invoices instanceof \Illuminate\Contracts\Pagination\Paginator)<div class="p-4">{{ $invoices->links() }}</div>@endif</section>
