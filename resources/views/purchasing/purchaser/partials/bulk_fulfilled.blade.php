@foreach ($fulfilledSummary as $summary)
    <label class="product-item block relative min-w-0 overflow-hidden rounded-2xl border border-slate-200 bg-white p-3.5 shadow-sm transition hover:bg-slate-50 cursor-pointer" data-tab="fulfilled" data-name="{{ $summary['product_name'] }}" data-sku="{{ $summary['sku'] }}" data-category="{{ $summary['category_name'] }}" data-frequent="{{ $summary['is_frequent'] ? 'true' : 'false' }}">
        <div class="flex items-center gap-3"><div class="flex items-center shrink-0"><input type="checkbox" name="product_ids[]" value="{{ $summary['product_id'] }}" class="product-checkbox h-5 w-5 rounded border-slate-300 text-teal-600 focus:ring-teal-500 cursor-pointer"></div><div class="min-w-0 flex-1"><div class="flex flex-wrap items-center gap-2"><h3 class="min-w-0 break-words font-black text-slate-900 text-sm opacity-60">{{ $summary['product_name'] }}</h3><span class="rounded-full bg-slate-100 px-2 py-0.5 text-[9px] font-black uppercase tracking-wider text-slate-500">{{ $summary['category_name'] ?: 'Other' }}</span></div><div class="mt-2 flex items-center gap-4 text-xs font-semibold text-slate-400"><span>Need: {{ number_format($summary['total_approved_qty'], 1) }} {{ $summary['unit'] }}</span><span>Bought: {{ number_format($summary['bought_qty'], 1) }}</span><span>Left: {{ number_format($summary['remaining_qty'], 1) }}</span><span class="rounded-full bg-emerald-100 px-2 py-0.5 text-[8px] font-black uppercase tracking-wider text-emerald-700">Fulfilled</span></div></div></div>
    </label>
@endforeach
